using UnityEngine;
using System.Collections;

public class BurgerObserver : MonoBehaviour {

	
	// This drag wasn't like the OP wanted. function OldDrag() { var ray : Ray = Camera.main.ScreenPointToRay(Input.mousePosition); grabbed.position = ray.origin + ray.direction * grabDistance; } 
}
